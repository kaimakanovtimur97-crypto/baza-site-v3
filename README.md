# БАЗА — многостраничный статический сайт

Готовый сайт на чистом HTML, CSS и JavaScript. Серверный код, PHP и формы заявок не используются: посетители связываются напрямую через Telegram, телефон или email.

## Основные страницы

| Файл | Страница |
|---|---|
| `index.html` | Главная |
| `uslugi.html` | Услуги |
| `vnedrenie-ii-v-biznes/` | Внедрение ИИ в бизнес (основной чистый адрес) |
| `o-nas.html` | О нас |
| `otzyvy.html` | Отзывы |
| `keisy.html` | Кейсы |
| `kontakty.html` | Контакты |
| `404.html` | Страница не найдена |

Все внутренние страницы используют относительные ссылки и готовы к публикации из корня домена. Старый адрес `vnedrenie-ii-v-biznes.html` сохранён как совместимый переход на чистый маршрут.

## Контакты вместо форм

- Telegram: `https://t.me/atanas_05`
- Телефон: `+7 916 635-36-05`
- Email: `atanasov5nikita@gmail.com`

Якоря `#form` и `#zayavka` сохранены для совместимости со старыми CTA, но ведут к блокам прямой связи и не собирают персональные данные на сайте.

## Публикация в Timeweb App Platform

Создайте приложение типа **Frontend → HTML/CSS/JS** и подключите репозиторий `kaimakanovtimur97-crypto/baza-site-v3`.

- ветка: `main`;
- путь до директории проекта: оставить пустым, потому что `index.html` находится в корне;
- команда и директория сборки: не используются;
- SPA fallback: выключить — сайт многостраничный;
- сжатие: включить gzip и zstd;
- основной подключённый домен: `база-биззнес.рф`;

Frontend-приложение отдаёт готовые файлы без Docker-контейнера и отдельного PHP-сервера.

## Локальная проверка

```sh
python3 -m http.server 4173 --bind 127.0.0.1
```

После запуска откройте `http://127.0.0.1:4173/`.
